{
  "name": "binary-websocket",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker that relays binary audio frames to an FFT visualiser.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260411.1",
    "typescript": "^6.0.2",
    "wrangler": "^4.81.1"
  },
  "dependencies": {
    "agents": "^0.13.3"
  }
}

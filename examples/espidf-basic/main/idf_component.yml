dependencies:
  # Local path so the example builds against the in-tree courier component.
  # Consumers copying this example should change this to a pinned release:
  #   inanimate/courier: ">=0.3.2"
  # Courier transitively pulls all required components — no other deps needed.
  courier:
    path: ../../..
